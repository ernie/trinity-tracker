# Binaries
bin/
*.exe
*.exe~
*.dll
*.so
*.dylib

# Database
*.db
*.db-shm
*.db-wal

# Config (keep example)
config.yml

# Logs
*.log

# Editor/IDE
.vscode/
.idea/
*.swp
*.swo
*~
.DS_Store

# Go
vendor/

# Claude
.claude/
CLAUDE.md
.superpowers/
docs/superpowers/

# Personal migration helpers (one-off, not for distribution)
scripts/migrate/

# Landing-page wallpaper variants are now committed alongside the
# source PNG. Re-run `bun run build:landing-images` only after the
# source wallpaper.png changes; the regular `bun run build` no longer
# regenerates them on every invocation.
