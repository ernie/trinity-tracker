// Stub — sections will be filled in across Phase 3 tasks.
// Renders a placeholder so the route works while we build out the rest.
export function LandingPage() {
  return (
    <div className="landing">
      <p style={{ padding: 80, textAlign: 'center', color: 'var(--text-dim)' }}>
        Landing page — under construction.
      </p>
    </div>
  )
}
