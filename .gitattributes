# Normalize line endings: store text as LF in the repo, check out LF on
# every platform. Keeps eslint/prettier (endOfLine: lf) happy regardless of
# a contributor's core.autocrlf setting. Binary files are auto-detected and
# left untouched.
* text=auto eol=lf
